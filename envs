export HOSTNAME=local
export APP=netcomp_sample
export VSN=01
export MOUNT=/opt
export CODE_LOADING_MODE=interactive
export FQDN=${HOSTNAME}.${APP}.nc.svc.cluster.local
export CURRENT_DIR=$( cd $( dirname ${BASH_SOURCE[0]} ) && pwd )
export ERLANG_HOME=/usr/local/Cellar/erlang/21.2.4/lib/erlang
